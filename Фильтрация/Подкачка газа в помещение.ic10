define kMaxPressure 101
define kMaxTemperature 30

alias temperature r15
alias pressure r14

Main:
yield
l temperature db TemperatureOutput
l pressure db PressureOutput
sub temperature temperature 273.15

slt r1 temperature kMaxTemperature
slt r2 pressure kMaxPressure
and r0 r1 r2
s db Mode r0

j Main